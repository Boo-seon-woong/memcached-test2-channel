## [YYYY-MM-DD HH:MM UTC / HH:MM KST] genie

### Result: <backend> <env> value=<value> nslots=<nslots>

Memnode command:

```sh
<command>
```

Memnode output:

```text
<paste key lines: region, slot size, attached>
```

Endpoint checks:

```sh
ip route get <service-ip>
nc -vz -w 3 <service-ip> 11212
```

Runner command:

```sh
<command>
```

Runner result summary:

```text
<paste ALL STATS or raw file paths>
```

Artifacts:

```text
<paths under transfer/ or run dir>
```

NEXT: ariel
